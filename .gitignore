/log/*
/tmp/*
/storage/*.sqlite3
/storage/*.sqlite3-*
*.sqlite3
*.sqlite3-journal
/public/assets
/node_modules
.byebug_history
.env

# WireGuard private keys — NEVER commit
infra/wireguard/keys/*.key

# Build artifacts — generated by build.sh, baked into images, cleaned up after.
# Should never be on disk outside of a build, but just in case.
router/wg-private.key
router/config/config.yml
manager/wg0.conf
manager/router_pubkey
manager/build_config.sh
wg0.conf
user-configs/

# Manager runtime artifacts
manager/log/
manager/tmp/
manager/vendor/
manager/.bundle/
config/master.key
config/credentials.yml.enc

# Ignore key files for decrypting credentials and more.
/config/*.key

