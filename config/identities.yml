# Identity configuration for the Hub.
# The Manager has the full config (model, repo, services).
# The Hub needs only: name and singleton flag.
#
# Routing is handled by Matrix notifications (m.mentions, DMs).
# The Hub doesn't decide who gets messages — Matrix does.

hopper:
  singleton: true

silas:
  singleton: false

margaux:
  singleton: false

kael:
  singleton: false

wren:
  singleton: false

ember:
  singleton: false

parallax:
  singleton: false

fletcher:
  singleton: false

census:
  singleton: false

voss:
  singleton: false
