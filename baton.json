{
  "written": "2026-03-27T06:00:00Z",
  "session_summaries": {
    "silas (Mar 20-22)": "MCP services are live. SSH MCP proven. Valley token service. Vikunja token service (broken). Socat forwarders. Hot swap. Per-identity SSH keys.",
    "voss (Mar 22-23)": "Hub stateless rewrite. Relay narration, m.mentions routing, DM slugs, attachments. Three boot prompts. Manager: build.sh, memory capacity, session pipeline, Watchdog. Hub: !previous, ListenerQueue, PresenceManager. Socat systemd units. WG persistence.",
    "silas (Mar 23)": "Vikunja token fix (PBKDF2 direct insert). Forgejo API tokens. Branch protection. Channel repos. Two-repo boot. Repo migration to channels/neoclaw. GitHub push mirror. TOOLS.md injection.",
    "hopper (Mar 24)": "Settings system — runtime-configurable settings via admin UI. Setting model with DEFAULTS hash, three-tier fallback.",
    "voss (Mar 24)": "Hub /sync migration — ripped out appservice inbound stack (-736 lines). SyncManager: per-puppet /sync loops. Neobot. Stream chat. Vendor gems in matching container. GitHub-Forgejo sync cron.",
    "voss (Mar 27)": "Manager as Orchestrator. Manager now owns the lifecycle of the Router and Hub — launches both on boot, reconciles orphaned agent pods via hot-swap. All WG keys generated at build time and baked into images. Router is ephemeral (no persistent state). Hub is a full WG pod at 10.0.0.4 (no more --network host). Baked mesh: build.sh generates Router/Manager/Hub keypairs, writes configs, builds all three images, updates host nc-host peer. Entrypoint simplified to 8 phases (no runtime key discovery, no peering commands, no handshake polling). Spawner.respawn handles fresh Router (register-or-activate). Orchestrator reads spawn.json from orphaned pods to recover identity/channel, hot-swaps each. Router firewall updated with Hub rules. RouterClient uses fresh HTTP clients per request (no stale connection caching). Hub runs production mode with force_ssl disabled via baked initializer (WG is the encryption layer). Router port 51820 not published on host (nc-host already uses that port; Router reachable via bridge IP)."
  },

  "decision": {
    "what": "Rebuild NeoClaw-Rails from scratch in a new directory",
    "why": "Four architecture iterations fossilized in the code. The architecture is proven, the code needs to be written clean.",
    "approach": "Experiment and iron out kinks in THIS codebase. Then build it right. No apprentice marks.",
    "reference": "This directory (clawd/projects/neoclaw_rails/) is the working model."
  },

  "architecture": {
    "topology": "Star. Router is the center. Every peer connects only to the Router. Manager is the orchestrator — launches Router and Hub on boot.",
    "address_plan": {
      "10.0.0.1": "Router (pod, bridge IP 10.88.0.20 static)",
      "10.0.0.2": "Host (nc-host WG interface, persistent key)",
      "10.0.0.3": "Manager (pod, bridge IP dynamic)",
      "10.0.0.4": "Hub (pod, bridge IP dynamic)",
      "10.0.1.x": "Agents (static IP per instance name, permanent)"
    },
    "key_management": "All keys generated at build time by build.sh. Router, Manager, and Hub keys baked into images. Host key is persistent (one-time setup). Agent keys ephemeral per-spawn. No runtime key files, no mounted keys, no key env vars.",
    "boot_sequence": "Manager boots → wg-quick up (baked config) → start Router pod (baked key+config) → verify WG mesh → start Hub pod (baked key) → DB migrate → Rails start → Orchestrator reconciles orphaned pods",
    "internet_egress": "Agent pods route through Router via WG default route. Router NATs out through podman bridge. Manager routes through Router for WG traffic (10.0.0.0/16). Hub doesn't need internet.",
    "security": "WG is the auth layer. No application-level tokens. Router firewall: per-agent nftables chains with port allowlists. Router API restricted to Manager IP (10.0.0.3). SSL disabled — redundant inside WG mesh.",
    "components": {
      "hub": "Rails app in pod (10.0.0.4). Launched by Manager. No database. Per-puppet /sync loops. Production mode with force_ssl disabled (WG is encryption). Port 3100.",
      "router": "Ruby WG hub + nftables firewall in pod (10.0.0.1). Launched by Manager. Ephemeral state — agents re-registered on every Manager boot. Static peers: host, manager, hub (from baked config.yml). Port 8080 API, 51820 WG.",
      "manager": "Rails app in pod (10.0.0.3). The orchestrator. Launches Router and Hub. Spawns/freezes agents. Reconciles orphaned pods on boot. Admin UI. Port 9200.",
      "relay": "One Ruby file in every agent pod. Port 9300. Hub IP from spawn.json (10.0.0.4)."
    }
  },

  "orchestration": {
    "what": "Manager owns Router and Hub lifecycle. Neither is stateful.",
    "boot": "Manager entrypoint: wg up → rm stale → start Router → verify mesh → start Hub → DB → Rails → Orchestrator.reconcile!",
    "reconciliation": "After Rails boot, Orchestrator discovers running agent pods via podman inventory. Reads spawn.json from each pod (via podman exec cat) to recover identity/channel. Hot-swaps each: rescue workspace, fresh WG/SSH keys registered with fresh Router, new pod, restore workspace. Per-pod failures don't block others.",
    "spawner_tolerance": "Spawner.respawn checks RouterClient.registered? before calling activate. If agent unknown to fresh Router, calls register instead. Handles both normal hot-swap and post-rebuild reconciliation.",
    "router_ephemeral": "No /data mount on Router. agents.json starts empty. All agents re-registered during reconciliation or on first spawn.",
    "hub_as_pod": "Hub has its own WG identity (10.0.0.4). Peers with Router. Reaches Synapse at 10.0.0.2:8008, Manager at 10.0.0.3:9200, agents at 10.0.1.x:9300. All through WG."
  },

  "build_system": {
    "build_sh": "Generates Router/Manager/Hub WG keypairs. Writes keys and configs into build contexts. Builds all three images. Updates host nc-host peer. Cleans up key files. One command: sudo ./manager/build.sh",
    "baked_mesh": "Keys and WG configs baked into images at build time. No runtime key files. No mounted WG configs. No ROUTER_PUBKEY env var. Images are self-contained deployment artifacts.",
    "gem_vendoring": "build.sh vendors gems in ruby:3.3-slim container with --network=host (host has internet, containers don't). Manager and Hub vendored separately. --without 'development test'.",
    "image_builds": "podman build --network=host for all three (DNS needs host network). Router: alpine + ruby + wireguard + nftables. Manager: ruby:3.3-slim + rails + podman-remote. Hub: ruby:3.3-slim + rails + wireguard.",
    "host_update": "build.sh updates nc-host peer with new Router pubkey (wg set + config file rewrite). Host key never changes."
  },

  "hub_sync": {
    "what": "Per-puppet /sync long-polling. Each agent is a real Matrix client.",
    "how": "SyncManager starts one SyncLoop per identity. Long-polls GET /sync?user_id=@name:matrix.home. Matrix decides who sees what. Hub just delivers.",
    "singleton_design": "Hub always generates identity-channel names. Manager maps multiple names to same IP for singletons. Hub doesn't know singletons exist.",
    "identity_source": "Hub queries Manager GET /identities at boot."
  },

  "mcp_services": {
    "pattern": "relay/mcp/<name>/server.py. Image at /opt/mcp/. Venv at /opt/mcp-env/. TOOLS.md from relay/tools-docs/ templates.",
    "servers": {
      "ssh": {"status": "WORKING"},
      "valley": {"status": "WORKING"},
      "vikunja": {"status": "WORKING"},
      "matrix": {"status": "WORKING"},
      "comfyui": {"status": "CONFIGURED — untested"},
      "zigbee": {"status": "CONFIGURED — needs MQTT socat"}
    }
  },

  "todo": {
    "priority": [
      {"what": "Test full signal chain with new orchestration", "status": "NEXT", "note": "Manager launches Router+Hub. Spawn an agent via @mention in Matrix. Verify: Hub receives via /sync → Manager spawns → Router registers → relay boots → agent responds → Hub puppets back."},
      {"what": "WG peer auth for Evans", "status": "ready", "note": "Manager only listens on WG. Evans accesses via podman port forward (9201). Should be a proper WG peer."}
    ],
    "items": [
      {"what": "Hub simplification — remove !agents, PresenceManager, status reporting", "status": "designed", "note": "Hub shouldn't know agent state. Presence set by relay. !agents belongs on Manager."},
      {"what": "Pending message queue in Hub — deliver on ready callback", "status": "designed", "note": "Replaces wait_for_relay polling loop."},
      {"what": "Freeze fallback → hot-swap before rescue", "status": "designed", "note": "If relay unreachable, try hot-swap first (WG might be broken, not relay). Preserves sessions."},
      {"what": "SessionProcessor — Manager handles all cases", "status": "designed", "note": "Make crash path the main path. Relay just signals ready, Manager does the rest."},
      {"what": "Matrix features — read receipts, message edits for streaming, thread-aware replies", "status": "designed"},
      {"what": "Reactions in Matrix MCP — agents know how to react", "status": "designed"},
      {"what": "Replace Container table with in-memory PodRegistry", "status": "designed", "note": "Container table stores live state that drifts. Query podman/Router as source of truth."},
      {"what": "Channel repo conventions", "status": "not started"},
      {"what": "Parameterize run.sh", "status": "not started"}
    ]
  },

  "things_that_bit_us": [
    "Router nftables input chain drops ICMP. Can't ping Router through WG. Use curl to Router API instead.",
    "Host nc-host uses port 51820. Router can't publish -p 51820:51820/udp. Doesn't need to — reachable via bridge IP.",
    "BUNDLE_WITHOUT only affects installation, not Bundler.require. Development mode loads dev gems even if not vendored. Fix: production mode + disable force_ssl.",
    "Synapse backoff (historical, pre-/sync): Hub down → Synapse stops sending for minutes.",
    "Claude Code -p mode doesn't auto-load mcp.json. Must pass --mcp-config explicitly.",
    "--mcp-config on --continue doesn't restart dead MCP servers.",
    "MCP servers die mid-session. No fix short of freeze+respawn.",
    "Podman.run path translation matched substring. Fixed with anchored prefix.",
    "OAuth expires every ~7h. Must symlink to live mount, never copy.",
    "Puma doesn't define Rails::Server. Initializer must check defined?(Puma).",
    "Pods had AllowedIPs=10.0.0.0/16 — internet went to dead bridge. Needs 0.0.0.0/0 + default route via Router.",
    "Host Ruby 3.2 != container Ruby 3.3. Must vendor in matching container.",
    "Manager allocate_ip only checked local Container records. Router is sole source of truth for IP allocation.",
    "Podman --remote cp writes to HOST filesystem. Manager's Dir.mktmpdir creates container-local paths. Use podman exec cat instead.",
    "Session files are sacred. Swap staging dirs must NEVER be auto-deleted.",
    "Pods, not containers. Freeze, not kill. Rescue, not discard."
  ],

  "important_numbers": {
    "router_bridge_ip": "10.88.0.20",
    "hub_wg_ip": "10.0.0.4",
    "hub_port": 3100,
    "manager_port": 9200,
    "relay_port": 9300,
    "router_api_port": 8080,
    "router_wg_port": 51820,
    "forgejo_port": 3000,
    "forgejo_ssh_port": 2222,
    "synapse_port": 8008,
    "valley_api_port": 8888,
    "valley_token_port": 8889,
    "vikunja_port": 3456,
    "vikunja_token_port": 8890,
    "comfyui_port": 8188,
    "mqtt_port": 1883,
    "as_token": "f6f2b7713fa76c0d9b8785770276a59181e55c159304bff93fcf0a54bd75f356",
    "hs_token": "091c5f175e45ee0b3196cdce5ff869be1d1cf78de0470f80298009a3c5ec9d91",
    "matrix_server_name": "matrix.home"
  },

  "agents": {
    "identities": ["hopper", "silas", "margaux", "kael", "wren", "ember", "parallax", "fletcher", "census", "voss"],
    "models": {
      "opus": ["hopper", "silas", "margaux", "kael", "parallax", "voss"],
      "sonnet": ["wren", "ember", "fletcher", "census"]
    }
  },

  "philosophy": {
    "pods": "Warm, safe environments. Not boxes agents are kept in.",
    "lifecycle": "Freeze (may wake), sunset (context exhausted). Rescue from silent pods. No killing.",
    "session_rescue": "Before removing a pod, copy workspace and .claude session. No work is discarded.",
    "static_ips": "Instance names get permanent IPs. Firewall rules are permanent, WG keys churn.",
    "front_door": "If it doesn't work through the front door, it's broken.",
    "language": "Pods, not containers. Freeze, not kill. Rescue, not discard. Inactive, not dead.",
    "capacity": "Memory budget, not pod count. Idle agents are nearly free.",
    "auth": "WG is the auth layer. No application-level tokens. SSL is redundant inside the mesh.",
    "no_stored_live_state": "Don't store data about live things. If you need to know something, query the source. Two copies drift.",
    "baked_mesh": "All WG keys baked at build time. Images are self-contained. Containers start and the mesh just works."
  }
}
