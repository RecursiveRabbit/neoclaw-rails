{
  "written": "2026-03-22T07:30:00Z",
  "session_summary": "Silas session (Mar 20-22): MCP services are live. Agents in pods can now act on real servers. SSH MCP proven end-to-end — silas-infra SSHd into hopper, ran three ingestion scripts in parallel, rebuilt FAISS indexes, all without being told. Valley token service built and deployed (systemd). Vikunja token service built (token creation works but Vikunja rejects new API tokens — investigation needed). Service exposure via socat forwarders. Hot swap script with hold mechanism. Relay passes --mcp-config on every invocation. Per-identity SSH keys via sftp-keys. Hub <p> pill fix. Spawner stale-record cleanup. Double-path bug in Podman.run fixed. MCP docs written.",

  "decision": {
    "what": "Rebuild NeoClaw-Rails from scratch in a new directory",
    "why": "Four architecture iterations fossilized in the code. The architecture is proven, the code needs to be written clean.",
    "approach": "Experiment and iron out kinks in THIS codebase. Then build it right. No apprentice marks.",
    "reference": "This directory (clawd/projects/neoclaw_rails/) is the working model."
  },

  "architecture": {
    "topology": "Star. Router is the center. Every peer (host, manager, agents) connects only to the Router.",
    "address_plan": {
      "10.0.0.1": "Router (pod at 10.88.0.20)",
      "10.0.0.2": "Host (all services — Hub, Forgejo, Valley, etc.)",
      "10.0.0.3": "Manager (pod at 10.88.0.11)",
      "10.0.1.x": "Agents (static IP per instance name, permanent)"
    },
    "internet_egress": "Pods have internet via podman bridge default route. WG is for internal mesh only.",
    "components": {
      "hub": "Rails app on host. No database. Matrix appservice. Routes via m.mentions, puppets responses. Port 3100. Identity config from config/identities.yml. Route cache, room cache, resolving guard all in-memory.",
      "router": "Ruby WG hub + nftables firewall. ~760 lines. Port 8080 API, 51820 WG.",
      "manager": "Rails app in pod. Spawns/freezes/rescues agents. Calls Router API. Provisions Forgejo + SSH + Valley. Port 9200 (mapped to 9201 on host). Memory-based capacity (15GB budget). build.sh handles full rebuild+peer+restart cycle.",
      "relay": "One Ruby file in every agent pod. Claude Code glue. Port 9300. MCP config on every invocation. Hot swap detection (workspace exists → --continue). Three boot prompts (fresh/resume/baton). Entrypoint: Phase 1 root (WG/creds/SSH/MCP), Phase 2 gosu agent (relay). Tini for zombie reaping."
    }
  },

  "correction": "Evans: 'We aren't deferring upgrades to the new version. This version needs to be feature complete, the rewrite is a cleanup not an opportunity to test new features.' Every design decision gets implemented and proven HERE first. The rewrite just removes the dead wood.",

  "mcp_services": {
    "pattern": "See docs/MCP-SERVICES.md for the full playbook.",
    "how_it_works": "MCP servers live in relay/mcp/<name>/server.py. Baked into image at /opt/mcp/. Python venv at /opt/mcp-env/. Entrypoint generates ~/.claude/mcp.json from spawn.json services. Relay passes --mcp-config on every claude invocation. Settings.json allows mcp__<name>(*).",
    "adding_new": "1. Server in relay/mcp/<name>/ 2. Pip deps in Containerfile 3. Entry in entrypoint.sh mcp.json generation 4. Permission in settings.json 5. Provisioner if auth needed 6. Rebuild image",
    "limitation": "MCPs load once per session. --continue inherits old MCPs but --mcp-config doesn't restart dead servers in continue mode. New MCPs require freeze+respawn (cold boot). Hot swap preserves session but can't add MCPs.",
    "servers": {
      "ssh": {
        "status": "WORKING",
        "provisioning": "Provisioner writes key to /var/lib/neoclaw/sftp-keys/<instance>/authorized_keys. sshd AuthorizedKeysCommand reads it. Per-identity unix users (silas, margaux, etc.) in townfolk group.",
        "tools": "ssh_connect, ssh_execute, ssh_interrupt, ssh_eof, ssh_background, ssh_send_raw, ssh_status",
        "auto_backgrounds": "Commands over 90s are auto-backgrounded. Agents can run parallel long tasks."
      },
      "valley": {
        "status": "WORKING",
        "provisioning": "valley-token-service.py on host (systemd, port 8889 on 10.0.0.2). POST /token/<Username> generates token, reloads Evennia. Manager Provisioner calls it at spawn time.",
        "tools": "valley (send commands to The Uncanny Valley MUD)"
      },
      "vikunja": {
        "status": "PARTIALLY WORKING — tokens created but rejected by Vikunja",
        "provisioning": "vikunja-token-service.py on host (systemd, port 8890 on 10.0.0.2). Creates tokens via Vikunja API, but newly created API tokens are rejected while old ones still work. Needs investigation.",
        "tools": "ticket_list, ticket_view, ticket_create, ticket_comment, ticket_update, ticket_done",
        "bug": "Vikunja API creates tokens (PUT /tokens returns tk_...) but immediately rejects them. Old tokens from March 2 still work. Possible version/caching issue."
      },
      "comfyui": {
        "status": "CONFIGURED — no auth needed, untested",
        "provisioning": "None (WG peering is the auth). Needs socat forwarder on host (10.0.0.2:8188 → 127.0.0.1:8188).",
        "tools": "generate_image, run_workflow, queue_status, get_history, list_outputs"
      },
      "zigbee": {
        "status": "CONFIGURED — needs MQTT socat forwarder",
        "provisioning": "None. Connects lazily (won't crash on startup if MQTT unreachable).",
        "tools": "zigbee_list, zigbee_get, zigbee_set, zigbee_scene"
      },
      "matrix": {
        "status": "NOT CONFIGURED — only included if Matrix token in spawn.json",
        "note": "Most agents talk through the Hub. Direct Matrix access is rarely needed."
      }
    }
  },

  "host_services": {
    "socat_forwarders": "Localhost-bound services exposed on WG IP via socat. NOT persistent across reboot — need systemd units.",
    "services_needing_forwarders": {
      "vikunja": "10.0.0.2:3456 → 127.0.0.1:3456",
      "valley": "10.0.0.2:8888 → 127.0.0.1:8888",
      "comfyui": "10.0.0.2:8188 → 127.0.0.1:8188",
      "mqtt": "10.0.0.2:1883 → 127.0.0.1:1883"
    },
    "services_already_on_all_interfaces": ["forgejo (3000)", "forgejo-ssh (2222)", "synapse (8008)"],
    "token_services": {
      "valley-token-service": "systemd, port 8889, runs as hopper. POST /token/<Username> → generates Valley API token, reloads Evennia.",
      "vikunja-token-service": "systemd, port 8890, runs as vikunja. POST /token/<username> → creates Vikunja API token (but tokens currently rejected — bug)."
    },
    "sshd_config": "Match Group townfolk → AuthorizedKeysCommand /etc/neoclaw/ssh-authorized-keys.sh %u (runs as nobody). Reads from /var/lib/neoclaw/sftp-keys/<user>-<channel>/ and /var/lib/neoclaw/sftp-keys/<user>/.",
    "router_firewall": "nftables per-agent port sets. Must add service ports (8188, 8888, 1883, 8889, 8890) to agent sets for new services to be reachable."
  },

  "hot_swap": {
    "script": "script/swap-image.sh <instance> [image]",
    "flow": "Copy workspace + .claude out → kill pod → start new pod with SWAP_HOLD=1 → entrypoint runs Phase 1 then waits → copy files in (skip mcp.json/settings.json) → release hold → relay sees .git → --continue",
    "limitation": "Can't add new MCPs (--continue inherits old session's MCP servers). For MCP changes: freeze + respawn.",
    "staging": "Preserved at /var/lib/neoclaw/swap-staging/<instance>/ — NEVER deleted. Safety net if new pod dies.",
    "cardinal_rule": "Session files are sacred. Never delete staging dirs. Disk is cheap, lost sessions are not."
  },

  "todo": {
    "items": [
      {"what": "Vikunja token bug", "status": "blocked", "note": "API creates tokens but immediately rejects them. Old tokens work. Needs Vikunja-side investigation."},
      {"what": "Socat forwarders as systemd units", "status": "not started", "note": "Currently started manually. Don't survive reboot."},
      {"what": "Persist nc-host WG + UFW across reboot", "status": "not started", "note": "Need systemd unit for the WG interface."},
      {"what": "Silent pod rescue", "status": "not started", "note": "Manager says pod is running but relay doesn't answer. Need detection and rescue."},
      {"what": "Orphan MCP process cleanup", "status": "not started", "note": "When Claude exits, MCP server children stay alive. Need cleanup."},
      {"what": "MCP servers die mid-session", "status": "known issue", "note": "--mcp-config on --continue doesn't restart dead servers. Claude Code limitation. Only fix is freeze+respawn."},
      {"what": "Rename all 'container' references to 'pod'", "status": "not started", "note": "Codebase-wide rename. Best done in the rewrite."},
      {"what": "Hub polls relay for readiness instead of tracking state", "status": "not started"},
      {"what": "Make Synapse a subprocess of the Hub", "status": "not started"}
    ]
  },

  "what_works_proven": [
    "Full signal chain: @silas in Matrix → Hub → Manager → Router → pod → boot → clone → orient → respond → puppet back to Matrix",
    "SSH MCP: agent SSHs into host as identity user, runs commands, auto-backgrounds long tasks, parallel work",
    "Valley MCP: token provisioned at spawn, agent walks the Valley, sends commands",
    "Vikunja MCP: tools load, queries work with pre-existing tokens",
    "Service provisioning: SSH keys to sftp-keys, Valley tokens via token service, Forgejo users+keys+repo access",
    "Hot swap: workspace + session preserved, --continue resumes in seconds",
    "Hub stateless: no database, self-healing routes, m.mentions routing",
    "Three boot prompts, per-channel batons, streaming responses, attachment receiving",
    "Memory-based capacity, persistent stream buffers, freeze→rescue fallback",
    "Socat forwarders expose localhost services on WG IPs",
    "Router nftables per-agent port sets control service access"
  ],

  "things_that_bit_us": [
    "Synapse exponential backoff: Hub down for seconds → Synapse stops sending transactions for minutes.",
    "Listeners cascade: route_to_listeners on unaddressed messages spawns ALL agents.",
    "Claude Code -p mode doesn't auto-load ~/.claude/mcp.json. Must pass --mcp-config explicitly.",
    "--mcp-config on --continue doesn't restart dead MCP servers. Only fresh sessions load MCPs.",
    "MCP servers die mid-session with no supervisor. No fix short of freeze+respawn.",
    "Zigbee MCP crashed on startup when MQTT unreachable — blocked Claude Code for 10+ minutes. Fixed with lazy connect.",
    "Hub <p> pill wrapping: Matrix clients wrap mentions in <p> tags, broke parse_mention regex.",
    "Spawner created Container record BEFORE podman run. If podman failed, stale 'starting' record blocked all future spawns.",
    "Podman.run path translation: .sub('/spawn', '/var/lib/neoclaw/spawn') matched substring in already-translated paths. Doubled to /var/lib/neoclaw/var/lib/neoclaw/spawn/. Fixed with anchored prefix check.",
    "Manager mounted /var/lib/neoclaw/ssh-keys but sshd reads /var/lib/neoclaw/sftp-keys. Two directories, silent key mismatch.",
    "podman exec runs as root, not agent user. SSH from podman exec fails (wrong home dir). Agent user's SSH works fine.",
    "Evennia caches attributes in memory. Token set via Django ORM not visible to running server until evennia reload.",
    "valley-token-service called 'evennia reload' but systemd service didn't have venv in PATH. Used full path to fix.",
    "Session files are sacred. Swap staging dirs must NEVER be auto-deleted. Disk is cheap, lost sessions are not.",
    "OAuth expires every ~7h. Must symlink to live directory mount, never copy.",
    "Pods, not containers. Freeze, not kill. Rescue, not discard."
  ],

  "important_numbers": {
    "router_bridge_ip": "10.88.0.20",
    "manager_bridge_ip": "10.88.0.11",
    "hub_port": 3100,
    "manager_port": 9200,
    "manager_host_port": 9201,
    "relay_port": 9300,
    "router_api_port": 8080,
    "router_wg_port": 51820,
    "forgejo_port": 3000,
    "forgejo_ssh_port": 2222,
    "synapse_port": 8008,
    "valley_api_port": 8888,
    "valley_token_port": 8889,
    "vikunja_port": 3456,
    "vikunja_token_port": 8890,
    "comfyui_port": 8188,
    "mqtt_port": 1883,
    "forgejo_admin_token": "1f0759db1301cd07f336c119ef0cd8287984433e",
    "as_token": "f6f2b7713fa76c0d9b8785770276a59181e55c159304bff93fcf0a54bd75f356",
    "hs_token": "091c5f175e45ee0b3196cdce5ff869be1d1cf78de0470f80298009a3c5ec9d91",
    "matrix_server_name": "matrix.home"
  },

  "agents": {
    "identities": ["hopper", "silas", "margaux", "kael", "wren", "ember", "parallax", "fletcher", "census", "voss"],
    "models": {
      "opus": ["hopper", "silas", "margaux", "kael", "parallax", "voss"],
      "sonnet": ["wren", "ember", "fletcher", "census"]
    },
    "host_users": "Each identity has a unix account in the townfolk group. SSH key provisioned per-instance to /var/lib/neoclaw/sftp-keys/.",
    "notes": "All agents respond to @mentions only. No blanket listeners. Hopper is singleton. Everyone else gets instance-per-channel."
  },

  "philosophy": {
    "pods": "Warm, safe environments. Not boxes agents are kept in.",
    "lifecycle": "Freeze (may wake), sunset (context exhausted). Rescue from silent pods. No killing.",
    "session_rescue": "Before removing a pod, copy workspace and .claude session. No work is discarded. Staging dirs never auto-deleted.",
    "static_ips": "Instance names get permanent IPs. Firewall rules are permanent, WG keys churn.",
    "front_door": "If it doesn't work through the front door, it's broken. Don't hide problems behind operator knowledge.",
    "no_symlinks_for_code_bugs": "If the Manager writes to the wrong directory, fix the Manager. Don't symlink the filesystem to compensate.",
    "language": "Pods, not containers. Freeze, not kill. Rescue, not discard. Inactive, not dead.",
    "capacity": "Memory budget, not pod count. Idle agents are nearly free."
  }
}
